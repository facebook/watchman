machine:
  php:
    version: 5.6.18
  python:
    version: 2.7.5
  node:
    version: 0.10.33

test:
  override:
    - ./travis/run.sh

dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install g++-5
